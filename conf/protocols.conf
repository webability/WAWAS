<!--

Definition of protocols:
They are necesary for anything to work, you need at least one protocol per listener

Definition of modules:
Please do not remove static module, it is necessary for known mimes, text, html and images at least for web
mod_static uses mimes config

Order IS IMPORTANT for use, first to catch the request will process it
(for example, CGI must be before STATIC, or code may be sent as text)

-->
<wawasconfig>

  <protocol>
    <name>Telnet</name>
    <lib>ProtocolTelnet</lib>
    <module>
      <!-- mod_admin to administrate the WAWAS -->
      <name>mod_telnetadmin</name>
      <lib>ModuleTelnetAdmin</lib>
    </module>
  </protocol>
  <protocol>
    <name>FTP</name>
    <lib>ProtocolFTP</lib>
    <module>
      <!-- mod_admin to administrate the WAWAS -->
      <name>mod_ftp</name>
      <lib>ModuleFTP</lib>
    </module>
  </protocol>
  <protocol>
    <name>DNS</name>
    <lib>ProtocolDNS</lib>
    <module>
      <!-- mod_admin to administrate the WAWAS -->
      <name>mod_dns</name>
      <lib>ModuleDNS</lib>
    </module>
  </protocol>
  <protocol>
    <name>Mail</name>
    <lib>ProtocolMail</lib>
    <module>
      <!-- mod_admin to administrate the WAWAS -->
      <name>mod_mail</name>
      <lib>ModuleMail</lib>
    </module>
  </protocol>

</wawasconfig>
